apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "badminton-service.fullname" . }}-test-connection"
  labels:
    {{- include "badminton-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "badminton-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
