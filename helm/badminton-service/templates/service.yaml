apiVersion: v1
kind: Service
metadata:
  name: {{ include "badminton-service.fullname" . }}
  labels:
    {{- include "badminton-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "badminton-service.selectorLabels" . | nindent 4 }}
